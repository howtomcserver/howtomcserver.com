{
  "name": "howtomcserver",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d dist -o origin-pages"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "gridsome": "^0.7.0"
  }
}
